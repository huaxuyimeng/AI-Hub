# 03 · 差距对照与迁移建议

> **本报告定位**：把 **02-ai-news-daily-借鉴手册.md** 的范式**一一对应**到 aihub 的**具体文件 / 具体行**上。
> **配套阅读**：01-AIHub-当前架构诊断.md（aihub 现状）、02-ai-news-daily-借鉴手册.md（范式来源）

---

## 1. 一对一对照表

| 范式 | aihub 当前位置 | 需改 / 新增 | 详见 |
|---|---|---|---|
| **A 本地时区** | `src/server/routers/news.ts:42-46` 用 `setUTCHours` | 替换为 `localDate()` 工具 | §2.1 |
| **B 时间解析** | `src/lib/news/service.ts:75` 兜底错 | 拆出 `time-utils.ts` + 加测试 | §2.2 |
| **C 源健康度** | schema 缺字段、`applyHealth` 缺失 | 加字段 + 函数 | §2.3 |
| **D Fragile 源** | 无 | `sources.ts` 加 `fragile?` 字段 | §2.4 |
| **E 实体指纹** | `service.ts:163-180` Jaccard 太松 | 拆出 `entity-fingerprint.ts` | §2.5 |
| **F B站 yt-dlp** | 完全缺失 | 新增 `src/lib/bilibili/` 目录 | §2.6 |
| **G 写入保护** | 无 | `src/lib/bilibili/storage.ts` | §2.7 |
| **H 配置热更新** | 无 PUT 接口 | `newsRouter.updateSource` | §2.8 |
| **I 安全** | 部分有（CRON_SECRET） | 补 CSRF / 路径校验 | §2.9 |
| **J 内存索引** | DB LIKE 全表扫 | 用 tsvector / GIN 索引替代 | §2.10 |
| **K 注释体系** | 部分有（P-0-1） | 全代码库推广 | §2.11 |
| **L 原子写** | DB 事务替代 | 不需要 | — |
| **M 日期归档** | DB 行替代 | 不需要 | — |

---

## 2. 落地建议（按文件）

### 2.1 本地时区（范式 A）

**改动文件**：
1. 新增 `src/lib/utils/date-util.ts`
2. 修改 `src/server/routers/news.ts:42-46`
3. 修改 `src/lib/news/service.ts:340-350`（`getAvailableDates`）

**diff 示意**：

```diff
// src/server/routers/news.ts
- // 与 UsageStat.recordUsage 的 UTC 日界保持一致
- const dayStart = new Date();
- dayStart.setUTCHours(0, 0, 0, 0);
+ // K-2：与新闻归档保持本地时区日界
+ const dayStart = new Date();
+ dayStart.setHours(0, 0, 0, 0);  // 本地 0 点
```

```diff
// src/lib/news/service.ts §getAvailableDates
- for (const r of result) {
-   dates.add(r.publishedAt.toISOString().slice(0, 10));
- }
+ for (const r of result) {
+   dates.add(localDate(r.publishedAt));
+ }
```

**注意**：改动会**改变 UI 看到的日期**。需要：

1. 在 `publishedAt` 存储上**明确选用一种时区策略**（建议：DB 存 UTC、应用层用本地）
2. `createdAt` 不动（这是爬取时间，本来就该是 UTC）
3. UI 上展示时**全部用本地时区**格式化

**风险**：中。会影响所有用户看到的日期。需要充分测试。

---

### 2.2 时间解析 + 测试（范式 B）

**改动文件**：
1. 新增 `src/lib/utils/time-utils.ts`
2. 新增 `src/lib/utils/time-utils.test.ts`
3. 修改 `src/lib/news/service.ts:75`

**diff 示意**：

```diff
// src/lib/news/service.ts
- import { prismaBase as prisma } from '@/lib/db';
- import { CATEGORY_KEYWORDS, NEWS_SOURCES, type NewsSourceConfig } from './sources';
+ import { prismaBase as prisma } from '@/lib/db';
+ import { CATEGORY_KEYWORDS, NEWS_SOURCES, type NewsSourceConfig } from './sources';
+ import { parseRssPubDate, parseCnDateTime, parseUploadDate, parseTodayHM, parseMonthDay, timeUnknown } from '@/lib/utils/time-utils';

  function parseRssXml(xml: string): FetchedItem[] {
    ...
    while ((match = itemRegex.exec(xml)) !== null) {
      const block = match[1];
      ...
-     publishedAt: pubDate ? new Date(pubDate) : new Date(),
+     // P0-1 修复：解析失败显式为 null，绝不回退成 new Date()
+     const t = parseRssPubDate(pubDate ?? '');
+     publishedAt: t.ts ? new Date(t.ts) : null,
+     publishPrecision: t.precision,
      crawledAt: new Date(),
    }
    ...
  }
```

**Schema 配合**：

```prisma
// prisma/schema.prisma NewsItem
model NewsItem {
  ...
  publishedAt    DateTime
  publishPrecision String?  // 'exact' | 'date' | null
  ...
}
```

**测试**（关键 — 没测试就别改）：

```ts
// src/lib/utils/time-utils.test.ts
import { parseRssPubDate, parseCnDateTime, parseTodayHM, parseMonthDay, rewindIfFuture } from './time-utils'

describe('parseRssPubDate', () => {
  test('P0-1: 解析失败保持 null', () => {
    expect(parseRssPubDate('garbage')).toEqual({ ts: null, precision: null })
    expect(parseRssPubDate('')).toEqual({ ts: null, precision: null })
  })
  test('aitntnews 年份修复', () => {
    const NOW = new Date('2026-08-30T00:00:00+08:00').getTime()
    // 源站把年份写成 2001，但月/日/时/分真实
    const r = parseRssPubDate('Sat, 28 Aug 2001 09:21:00 +0800', NOW)
    expect(r.precision).toBe('exact')
    expect(new Date(r.ts!).getFullYear()).toBe(2026)
  })
  test('跨年回退', () => {
    const NOW = new Date('2027-01-05T10:00:00+08:00').getTime()
    const r = parseCnDateTime('2026年12月28日 09:21', NOW)
    expect(new Date(r.ts!).getFullYear()).toBe(2026)
  })
})

describe('parseTodayHM', () => {
  test('跨午夜回退到昨天', () => {
    const NOW = new Date('2026-08-30T00:30:00+08:00').getTime()
    const r = parseTodayHM('23:53', NOW)
    expect(new Date(r.ts!).getDate()).toBe(29)
  })
  test('当天正常', () => {
    const NOW = new Date('2026-08-30T15:00:00+08:00').getTime()
    const r = parseTodayHM('14:53', NOW)
    expect(new Date(r.ts!).getDate()).toBe(30)
  })
})
```

**风险**：低。前置测试充分。

---

### 2.3 源健康度（范式 C）

**改动文件**：
1. 修改 `prisma/schema.prisma NewsSource`
2. 新增 `src/lib/news/health.ts`
3. 修改 `src/lib/news/service.ts:fetchAllNews`
4. 修改 `src/server/routers/news.ts`（新增 health endpoint）
5. 修改 `src/app/api/cron/fetch-news/route.ts`（触发告警）

**Schema diff**：

```prisma
model NewsSource {
  ...
  lastFetchAt  DateTime?
  lastOkAt     DateTime?  // ← 新增
  lastCount    Int       @default(0)  // ← 新增
  emptyStreak  Int       @default(0)  // ← 新增
  failStreak   Int       @default(0)  // ← 新增
  failCount    Int       @default(0)  // 已有但没用
  lastError    String?  // ← 新增
  lastMs       Int?     // ← 新增
  unhealthy    Boolean  @default(false)  // ← 新增
  ...
}
```

**注意**：迁移现有数据时 `emptyStreak/failStreak/unhealthy` 默认 0/false，不会误报。

**service.ts 改动**：

```ts
// src/lib/news/service.ts §fetchAllNews
import { applyHealth } from './health'

export async function fetchAllNews(): Promise<...> {
  ...
  for (const chunk of chunks) {
    const chunkResults = await Promise.allSettled(...)
    for (let i = 0; i < chunkResults.length; i++) {
      const r = chunkResults[i]
      const cfg = chunk[i]
      const t0 = Date.now()
      if (r.status === 'fulfilled') {
        allItems.push(...r.value.items)
        results.push({ name: cfg.name, count: r.value.items.length, success: true })
        await applyHealth(cfg.name, { ok: true, count: r.value.items.length, ms: Date.now() - t0 })
      } else {
        results.push({ name: cfg.name, count: 0, success: false })
        await applyHealth(cfg.name, { ok: false, count: 0, ms: Date.now() - t0, error: String(r.reason) })
      }
    }
  }
  ...
}
```

**路由**：

```ts
// src/server/routers/news.ts
health: protectedProcedure.query(async () => {
  return prismaBase.newsSource.findMany({
    where: { enabled: true },
    orderBy: [{ unhealthy: 'desc' }, { name: 'asc' }],
    select: {
      id: true, name: true, type: true, enabled: true,
      lastFetchAt: true, lastOkAt: true, lastCount: true,
      emptyStreak: true, failStreak: true, lastError: true,
      unhealthy: true,
    },
  })
}),
```

**前端**：可加一个"数据源健康度"页面（在 settings 页面下）。

---

### 2.4 Fragile 源降级（范式 D）

**改动文件**：
1. 修改 `src/lib/news/sources.ts`
2. 修改 `src/lib/news/service.ts:fetchAllNews`

**diff 示意**：

```diff
// src/lib/news/sources.ts
  export interface NewsSourceConfig {
    name: string
    url: string
    type: 'rss' | 'api' | 'html'
    priority: number
    enabled?: boolean
    notes?: string
+   fragile?: boolean  // 失败是否仅警告
  }

  export const NEWS_SOURCES: NewsSourceConfig[] = [
    ...
    {
      name: 'AI Bot',
      url: 'https://ai-bot.cn/daily-ai-news/',
      type: 'html',
      priority: 7,
+     fragile: true,  // 正则爬虫改版率高
      notes: 'HTML 抓取，需要针对性解析',
    },
    ...
  ]
```

```diff
// src/lib/news/service.ts §fetchAllNews
+ const warnings: string[] = []  // 非阻断
+ const errors: string[] = []    // 阻断级
  const results = ...

  for (let i = 0; i < chunkResults.length; i++) {
    ...
    if (r.status === 'fulfilled') {
      ...
    } else {
      const msg = `${cfg.name}: ${String(r.reason)}`
+     if (cfg.fragile) warnings.push(msg)
+     else errors.push(msg)
      results.push({ name: cfg.name, count: 0, success: false })
    }
  }
```

**返回结构**：

```diff
  return {
    sources: results,
    totalItems: totalSaved,
+   warnings,
+   errors,
  }
```

---

### 2.5 实体指纹多源去重（范式 E）

**改动文件**：
1. 新增 `src/lib/news/entity-fingerprint.ts`
2. 新增 `src/lib/news/entity-fingerprint.test.ts`
3. 修改 `src/lib/news/service.ts:dedupeAndCross` → `dedupeAndCrossV2`

**关键修复**：

```ts
// 新模块独立可测
export function findMultiSourceRelations<T extends ...>(items: T[]): MultiSourceRelation[] {
  ...
}

// 替换原有的 Jaccard 太松
function dedupeAndCrossV2(items: FetchedItem[]): FetchedItem[] {
  // 第一步：粗合并（标题完全相同或 Jaccard ≥ 0.7）
  ...
  // 第二步：实体指纹跨组关联
  const relations = findMultiSourceRelations(...)
  // 写回 crossSources
  ...
}
```

**配套改动**：
- `extractModelNames` 也用类似 tokenize（避免重复正则）
- 阈值 3 应作为可配置常量（环境变量 `MULTI_SOURCE_MIN_SHARED`）

---

### 2.6 B站 yt-dlp（范式 F）

**改动文件**（新增模块）：
1. `src/lib/bilibili/scraper.ts` — yt-dlp 子进程封装
2. `src/lib/bilibili/parser.ts` — 文章解析（橘鸦/黑鸦两种格式）
3. `src/lib/bilibili/storage.ts` — Prisma 持久化
4. `src/lib/bilibili/watermark.ts` — 写入保护
5. `prisma/schema.prisma BilibiliCache` — 新增 model
6. `src/app/api/cron/fetch-bilibili/route.ts` — 新增 cron
7. `vercel.json` — 新增 cron 配置
8. `src/server/routers/bilibili.ts` — 新增 tRPC 路由

**Schema 改动**：

```prisma
model BilibiliCache {
  id          String   @id @default(uuid())
  fetchedAt   DateTime @default(now())
  note        String?  // "橘鸦Juya 沿用历史缓存（本次受 B 站风控影响）"
  ups         Json     // [{uid, name, tag, videos: [{bvid, title, ...}]}]
  sourceHealth Json?  // {uid: {ok, count, ms, error}}
  @@index([fetchedAt])
}
```

**详细方案见报告 06**。

---

### 2.7 写入保护（范式 G）

并入 §2.6 的 `src/lib/bilibili/watermark.ts` 实现。

---

### 2.8 配置热更新（范式 H）

**改动文件**：
1. 修改 `src/server/routers/news.ts`

**新增 endpoint**：

```ts
// src/server/routers/news.ts
const sourceUpdateSchema = z.object({
  id: z.string(),
  enabled: z.boolean().optional(),
  priority: z.number().int().min(0).max(100).optional(),
  fragile: z.boolean().optional(),
  notes: z.string().max(500).optional(),
})

updateSource: protectedProcedure
  .input(sourceUpdateSchema)
  .mutation(async ({ input }) => {
    const { id, ...patch } = input
    return prismaBase.newsSource.update({
      where: { id },
      data: patch,
    })
  }),

createSource: protectedProcedure
  .input(z.object({
    name: z.string().min(1).max(100),
    url: z.string().url(),
    type: z.enum(['rss', 'html', 'api']),
    priority: z.number().int().min(0).max(100).default(1),
    fragile: z.boolean().default(false),
    notes: z.string().max(500).optional(),
  }))
  .mutation(async ({ input }) => {
    return prismaBase.newsSource.create({ data: input })
  }),

deleteSource: protectedProcedure
  .input(z.object({ id: z.string() }))
  .mutation(async ({ input }) => {
    return prismaBase.newsSource.delete({ where: { id: input.id } })
  }),
```

---

### 2.9 安全细节（范式 I）

**改动文件**：
1. 修改 `src/app/api/cron/fetch-news/route.ts`
2. 修改 `src/app/api/cron/refresh-models/route.ts`

**diff 示意**：

```diff
// src/app/api/cron/fetch-news/route.ts
  if (expected && authHeader !== `Bearer ${expected}`) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }

+ // H-3：不回传内部错误细节（防路径泄漏）
+ let userMessage: string | null = null
  try {
    const result = await fetchAllNews();
    ...
    return NextResponse.json({
      ok: true,
      duration,
      totalItems: result.totalItems,
      sources: result.sources,
+     warnings: result.warnings?.length ?? 0,
+     errors: result.errors?.length ?? 0,
      timestamp: new Date().toISOString(),
    });
  } catch (err) {
    console.error('[cron] News fetch failed:', err);  // ← 服务端日志详细
-   return NextResponse.json({ error: (err as Error).message }, { status: 500 });
+   // 客户端只看到"内部错误"，详情在服务端日志
+   return NextResponse.json({ error: 'Internal error', code: 'NEWS_FETCH_FAILED' }, { status: 500 });
  }
```

---

### 2.10 搜索改造（范式 J 替代）

**改动文件**：
1. 修改 `prisma/schema.prisma NewsItem`
2. 修改 `src/server/routers/news.ts queryNews`

**方案 A — 简单**：加 `@@index` 和全文搜索字段（SQLite 用 FTS5，Postgres 用 tsvector）

```prisma
// Postgres
model NewsItem {
  ...
  // 全文搜索（生产用 tsvector，开发可省略）
  searchVector Unsupported("tsvector")?  // Postgres only
  @@index([searchVector], type: Gin)
}
```

**方案 B — 实用**：用 Prisma 的 `mode: 'insensitive'` + 索引

```prisma
model NewsItem {
  ...
  // 用 SQLite 的 COLLATE NOCASE + 索引
}
```

```ts
// src/lib/news/service.ts queryNews
if (options.search) {
  where.OR = [
    { title: { contains: options.search } },
    { summary: { contains: options.search } },
    // 扩展：搜索 crossSources、relatedModels
    { relatedModels: { contains: options.search } },
    { crossSources: { contains: options.search } },
  ]
}
```

**方案 C — 高级**（详见报告 07）：意图搜索

```ts
// 解析用户输入"性价比高的中文模型"
// → 展开成 (provider IN ['DeepSeek', 'Alibaba', 'Moonshot']) AND (priceInput < 5)
// → 加上向量召回
```

---

### 2.11 注释体系（范式 K）

**约定**（写入 README）：

```md
## 注释编号约定

- **P0-X**：数据正确性修复（必须）
- **P1-X**：数据完整性 / 体验改进
- **P2-X**：差异化能力
- **P3-X**：规模化 / 工程化
- **H-X**：安全 / 健壮性加固
- **E-X**：错误处理修正
- **K-X**：跨文件一致性
- **W-X**：已知缺陷（暂时不修）
```

**示例**：

```ts
// P0-1 修复：RSS pubDate 解析失败时保持 null
// K-3：uid 统一用字符串，与 fetch_bilibili.py 对齐
// H-7：cron route 不回传内部错误详情（防路径泄漏）
```

---

## 3. 迁移顺序（强建议）

### Sprint 1 — 数据正确性（4-5 天）

| # | 任务 | 文件 | 工作量 |
|---|---|---|---|
| 1 | 新增 `time-utils.ts` + 测试 | 新增 | 1.5 天 |
| 2 | 修 P0-1（pubDate null） | `news/service.ts:75` | 0.5 天 |
| 3 | 修 P0-2（HTML 白名单） | `news/service.ts:131` | 0.5 天 |
| 4 | 修 P0-4（Jaccard → 实体指纹） | 新增 `entity-fingerprint.ts` + 改 `service.ts` | 2 天 |
| 5 | 加 `date-util.ts` + 修 `news.ts` stats | 新增 + 改 | 0.5 天 |

### Sprint 2 — 可观测性（3-4 天）

| # | 任务 | 文件 | 工作量 |
|---|---|---|---|
| 6 | Schema 加源健康度字段 | `prisma/schema.prisma` | 0.5 天 |
| 7 | `applyHealth` 函数 + 集成 | 新增 + 改 `service.ts` | 1 天 |
| 8 | `newsRouter.health` endpoint | 改 `news.ts` | 0.5 天 |
| 9 | Cron 不回传错误细节 | 改 cron route | 0.5 天 |
| 10 | 结构化日志（JSON） | 改 cron route | 1 天 |

### Sprint 3 — 健壮性（2 天）

| # | 任务 | 文件 | 工作量 |
|---|---|---|---|
| 11 | Fragile 源标记 | 改 `sources.ts` + `service.ts` | 0.5 天 |
| 12 | 并发限流 | 改 `service.ts` | 1 天 |
| 13 | 批量 upsert | 改 `service.ts:saveItems` | 0.5 天 |

### Sprint 4 — 配置化（1 天）

| # | 任务 | 文件 | 工作量 |
|---|---|---|---|
| 14 | updateSource endpoint | 改 `news.ts` | 0.5 天 |
| 15 | 前端"数据源管理"页面 | 新增 UI | 0.5 天 |

### Sprint 5 — 模型爬虫（3-4 天）

| # | 任务 | 文件 | 工作量 |
|---|---|---|---|
| 16 | 厂商定价解析（真抓官网） | 改 `scraper.ts` | 2 天 |
| 17 | AA 真接 | 改 `scraper.ts` | 1 天 |
| 18 | discoverFromNews 复用标题正则 | 改 `scraper.ts` | 1 天 |

### Sprint 6+ — 差异化（详见报告 09）

---

## 4. 不要做的事

| 不要做 | 为什么 |
|---|---|
| ❌ 引入 Elasticsearch / Meilisearch | DB + 索引足够，加 ES 是过度设计 |
| ❌ 把 aihub 拆成 monorepo | 单仓够用，拆了增加复杂度 |
| ❌ 把 ai-news-daily 的全部代码搬到 aihub | DSH 插件形态不适配 Next.js |
| � 引入 Kafka / Redis Streams | 单体 cron 够用 |
| ❌ 用 GraphQL 替换 tRPC | tRPC 已定型 |

---

## 5. 验证清单（每个 Sprint 完成后跑）

- [ ] `npx tsc --noEmit` 通过
- [ ] `npx prisma validate` 通过
- [ ] 新增/修改的单元测试通过
- [ ] 手动触发一次 `/api/cron/fetch-news`，看 `warnings / errors` 字段
- [ ] 手动触发一次 `/api/cron/refresh-models`，看 `discovered / updated / failed`
- [ ] UI 检查：日期切换正常、新闻列表正常、模型排行正常

---

**下一步**：阅读 **04-新闻聚合爬虫-深度诊断.md** 了解新闻模块的具体每个 Bug。